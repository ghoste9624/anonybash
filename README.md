# anonybash
```
apt update && apt full-upgrade -y && pkg update && pkg upgrade -y && pkg install git zsh -y && git clone https://github.com/ghoste9624/anonybash && cd anonybash && chmod +× anon.sh && bash anon.sh
```
